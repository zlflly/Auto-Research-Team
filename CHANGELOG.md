# Changelog

## 0.1.1 — repository-management customization

Based on the user's supplied 0.1.0 package; this is a local customization, not an upstream release claim.

- Keep the original native Owner/four-role model, bounded two-candidate search and single managed evaluation slot.
- Add a repository contract, standalone AGENTS.md asset, explicit workspace leases and fixed baseline/A/B slot examples.
- Reuse nodes/team/runs as sources of truth; derive an inventory instead of adding an independent branch registry.
- Add repoctl seal/check/catalog. Preserve declared source/config bytes and file modes per node, including negative candidates.
- Add the source_snapshot_policy=required protocol profile and run/evidence/finish snapshot-binding checks.
- Keep legacy behavior for older frozen protocols; require a new linked task to change evaluation contracts.
- Integrate Owner/Engineer handoff, source scope, no-rebase-after-freeze, integration re-evaluation and non-destructive archival guidance.
- Add local unit/integration fixtures. No Codex-host native-agent end-to-end result is claimed.
- Do not change installer overwrite policy, global host settings, permissions, agent credentials, or introduce automatic cleanup/push.
